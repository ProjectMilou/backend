version: "3"
services:
  api-server:
    build: ./api-server
    depends_on:
      - mongo
    ports:
      - "49160:8080"
  mongo:
    image: library/mongo:3.6.19-xenial
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
    driver: local